<doc>
	<legacy_id>2393</legacy_id>
	<name>delete stack</name>
	<type>command</type>

	<syntax>
		<example>delete <i>stack</i></example>
	</syntax>

	<synonyms>
		<synonym>clear stack</synonym>
	</synonyms>

	<summary>Removes a <glossary tag="substack">substack</glossary> from a <glossary tag="stack file">stack file</glossary>, or removes a <glossary tag="main stack">main stack</glossary> from memory.</summary>

	<examples>
<example>delete stack "Expressions"</example>
	</examples>

	<history>
		<introduced version="1.0">Added.</introduced>
		<deprecated version="6.2">6.2</deprecated>
		<removed version=""></removed>
		
		<experimental version=""></experimental>
		<nonexperimental version=""></nonexperimental>
	</history>

	<objects>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
		<ios/>
		<android/>
	</platforms>

	<classes>
		<desktop/>
		<server/>
		<web/>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
		<category>Objects &amp; Messages</category>
	</classification>

	<references>
		<command tag="delete">delete Command</command>
		<command tag="purge stack">purge mainstack from memory</command>
		<command tag="remove stack">remove substack from mainstack in memory</command>
		<property tag="cantDelete">cantDelete Property</property>
		<property tag="destroyStack">destroyStack Property</property>
		<property tag="purgeStack">purgeStack property</property>
		<message tag="deleteStack">deleteStack Message</message>
	</references>

	<description>Use the <b>delete stack</b> <glossary tag="command">command</glossary> to delete a <glossary tag="substack">substack</glossary>, or to purge a <glossary tag="main stack">main stack</glossary> from memory.<p></p><p><b>Parameters:</b></p><p>The <i>stack</i> is any valid stack reference.</p><p></p><p><b>Comments:</b></p><p>The <b>delete stack</b> <glossary tag="command">command</glossary> has different effects, depending on whether the <i>stack</i> is a <glossary tag="main stack">main stack</glossary> or a <glossary tag="substack">substack</glossary>.</p><p></p><p>If the <i>stack</i> is a <glossary tag="main stack">main stack</glossary>, the <b>delete stack</b> <glossary tag="command">command</glossary> removes the <object tag="stack">stack</object> from memory. (Any unsaved changes are lost.) However, it does not remove it from the user's system. To delete a <glossary tag="main stack">main stack</glossary>, use the <command tag="delete file">delete file</command> <glossary tag="command">command</glossary> instead.</p><p></p><p></p><caution>If the <i>stack</i> is a <glossary tag="substack">substack</glossary>, the <b>delete stack</b><glossary tag="command">command</glossary> deletes the <object tag="stack">stack</object> from memory, which causes it to be deleted permanently the next time its <glossary tag="stack file">stack file</glossary> is saved.</caution></description>
</doc>